<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Critical Mexico marine endangered species Map</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <style>
        #map {
            height: 90vh;
            width: 100%;
        }
    </style>
</head>

<body>
    <div id="map"></div>
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
    <script>
        const mexicodata = <%- mexicomap %>;

        const map = L.map('map').setView([23.6345, -102.5528], 5); // Example coordinates for Mexico;

        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            maxZoom: 19,
            attribution: '© OpenStreetMap'
        }).addTo(map);

        fetch('data/iucnredlist-mexico-data.json').then(response => response.json())
            .then(data => {
                data.iucnspecies.forEach(feature => {
                    const popupHtml = `<img src="${feature.image}" style="width:100px;height:100x;"> <br> ${feature.url ? `<a href="${feature.url}" target="_blank" rel="noreferrer">${feature.speciesname}</a>` : ''}`;
                    L.marker([feature.coordinates.lat, feature.coordinates.lng])
                        .addTo(map)
                        .bindPopup(popupHtml);
                });
            })
            .catch(error => console.error('Error fetching data:', error));
    </script>
</body>

</html>